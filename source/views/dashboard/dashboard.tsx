import { Card, CardContent, CardHeader, CardTitle } from "@/theme/components/ui/card";
// import LayoutDialog from "./components/layout-dialog";

export default () => (
  <div className="space-y-4 p-4 size-full mx-auto flex flex-col gap-4 items-center justify-center">
    
      <CardHeader>
        <CardTitle className="text-2xl font-bold">Welcome</CardTitle>
      </CardHeader>
      <CardContent>
      <CardTitle>Dashboard Overview</CardTitle>
      </CardContent>
  
</div>
);
